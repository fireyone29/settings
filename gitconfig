[user]
	name = fireyone29
	email = fireyone29@server
[credential]
	helper = cache
[merge]
	ff = false
[alias]
	co = checkout
	ci = commit
	st = status
	sto = log @{u}.. --oneline
	sti = log ..@{u} --oneline
	br = branch
	hist = log --pretty=format:\"%h %ad | %s%d [%an]\" --graph --date=short
	type = cat-file -t
	dump = cat-file -p
[color]
	ui = auto
[color "branch"]
       current = normal reverse
       local = normal
       remote = green blink
[color "diff"]
       meta = cyan 
       frag = magenta bold
       old = red bold
       new = green bold
[color "status"]
       added = yellow
       changed = red
       untracked = cyan
